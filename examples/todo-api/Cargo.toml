[package]
name = "jigs-example-todo-api"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
jigs = { path = "../../crates/jigs", features = ["trace"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
