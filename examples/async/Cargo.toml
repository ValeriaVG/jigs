[package]
name = "jigs-example-async"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
jigs = { path = "../../crates/jigs", features = ["trace"] }
tokio = { version = "1", features = ["rt", "macros", "time"] }
